package Model;

public enum ProjectStatusType {
    //TODO usunąc project status z tabelki project, stworzyc nowa tabele projectc status + to samo dla tabeli Binary

    NEW,
    ONGOING,
    CANCELED,
    CLOSED

}
